<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ProjectAppealMapper">
   <select id="selectLastAppealByProjectId" resultType="org.example.entity.ProjectAppeal">
      select * from project_appeal where project_id = #{pid} order by id desc limit 1
   </select>
            
</mapper>